# Импортируем модуль random
import random

# Спрашиваем пользователя, сколько костей он хочет бросать, и получаем его ответ с помощью функции input()
dice_num = input('Сколько костей вы хотите бросать? ')
# Присваиваем ответ пользователя переменной dice_num и приводим его к целому числу с помощью функции int()
dice_num = int(dice_num)

# Спрашиваем пользователя, сколько раз он хочет бросать кости, и получаем его ответ с помощью функции input()
dice_rolls = input('Сколько раз вы хотите бросать кости? ')
# Присваиваем ответ пользователя переменной dice_rolls и приводим его к целому числу с помощью функции int()
dice_rolls = int(dice_rolls)

# Создаем пустой список dice_results для хранения результатов бросков костей
dice_results = []

# Начинаем цикл for, который выполняется dice_rolls раз
for i in range(dice_rolls):
    # Создаем переменную roll_sum и присваиваем ей значение 0
    roll_sum = 0
    # Начинаем вложенный цикл for, который выполняется dice_num раз
    for j in range(dice_num):
        # Генерируем случайное число от 1 до 6 и присваиваем его переменной dice_value
        dice_value = random.randint(1, 6)
        # Прибавляем dice_value к roll_sum
        roll_sum += dice_value
    # Заканчиваем вложенный цикл for
    # Добавляем roll_sum в список dice_results с помощью метода append()
    dice_results.append(roll_sum)
# Заканчиваем внешний цикл for

# Выводим список dice_results на экран с помощью функции print()
print(f'Результаты бросков костей: {dice_results}')

